name: Terraform

on:
  push:
    branches:
      - main
  pull_request:

defaults:
  run:
    working-directory: ./environments/prod
        
jobs:
  terraform-workflow:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      pull-requests: write

    steps:
      # Workload Identity連携
      - uses: actions/checkout@v4
      - id: 'auth'
        name: 'Authenticate to Google Cloud'
        uses: 'google-github-actions/auth@v2.1.0'
        with:
          token_format: access_token
          access_token_lifetime: 900s
          # TODO: workload_identity_providerを自分の環境に合わせて変更する
          workload_identity_provider: 'projects/1021452053185/locations/global/workloadIdentityPools/terraform-pool/providers/terraform-pool-provider'
          # workload_identity_provider: 'projects/xxxxxxx/locations/global/workloadIdentityPools/tosxxxx-pool/providers/xxxxx-pool-provider'
          # TODO: service_accountを自分の環境に合わせて変更する
          service_account: 'terraform@gpj-gd3-dev-kurihara.iam.gserviceaccount.com'
          # service_account: 'terraform@gxxxxxxx.iam.gserviceaccount.com'
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.5.4

      - name: Terraform Fmt
        id: fmt
        run: terraform fmt -check -recursive
        continue-on-error: true
          
      - name: Terraform Init
        id: init
        run: TF_VAR_ACCESS_TOKEN=${{ steps.auth.outputs.access_token }} terraform init
      
      - name: Terraform Validate
        id: validate
        run: terraform validate -no-color
        
      - name: Terraform Plan
        id: plan
        run: terraform plan -no-color
        
        
      - name: Terraform Apply
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: terraform apply -auto-approve -input=false -var "gh_private_key=${{ secrets.TF_VAR_GITHUB_TOKEN }}"
